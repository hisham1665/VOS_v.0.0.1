[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aos-v0"
version = "0.0.5"
description = "A capability-routed, auditable agent execution kernel"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0,<3.0",
    "requests>=2.31.0,<3.0",
    "python-dotenv>=1.0.0,<2.0",
    "groq>=0.13.0",
    "openai>=1.0.0",
    "ddgs>=9.0.0",
    "huggingface_hub[inference]>=0.35.0",
    "pdfplumber>=0.11.0",
]

[project.scripts]
aos = "aos_v0.cli:main"
aos-tui = "aos_v0.tui:main"
aos-registry-export = "aos_v0.services.registry_export:main"

[project.optional-dependencies]
tui = ["textual>=0.58,<2.0", "rich>=13,<15"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-dir]
"" = "src"
